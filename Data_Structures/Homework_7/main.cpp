#include <iostream>
#include "HashTable.hpp"

using namespace std;

int main(int argc, char argv[]) {
  if(argc != 4) {
    cout << "Invalid number of arguments." << endl;
  }
  else {
    
  }
}
